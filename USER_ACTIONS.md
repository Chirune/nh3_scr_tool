# 你在第一阶段需要做什么

程序开发、批量运行、结果汇总和错误回归由 Codex 负责。你只处理需要 NH3-SCR 专业判断的审核任务。

## 当前需要操作

2020 和 2026 两篇原始研究论文的当前审核均已完成，暂时不需要继续修改原有审核表。

曲线结果位于 `output/pilot_2020_curves/`：先打开 `fig11/overlay.png`、`fig12/overlay.png`、`fig13/overlay.png` 看绿色编号是否准确落在数据标记上，再抽查 `all_curve_points.csv` 的数值。该汇总表有 454 个估读点，仍需审核后才能进入训练集。

下一步请向 `D:\Desktop\NH3 SCR\article` 增加 3–5 篇新的 NH3-SCR 原始研究论文，尽量同时包含补充材料；优先选择催化剂体系和出版商格式不同的论文。当前仍在多论文测试阶段，不要直接投入全部论文库。

## 审核对照表时

打开提供的审核表，逐行核对原文，只填写以下判断：

1. 该记录是否属于论文作者自己的实验或计算结果。
2. 催化剂名称是否对应正确样品，是否混用了载体、对照样或老化样。
3. 数值、单位和指标含义是否正确。
4. 反应条件是否与该性能数据属于同一次实验。
5. 活性中心结论属于直接测量、表征支持还是作者机理推断。
6. 无法确定时选择“待定”，不要猜测。

你不需要手工抄录整篇论文。优先检查程序已经列出的记录及程序标出的缺失字段。

## 多论文测试通过后

1. 确认正式纳入的论文范围，例如只纳入原始实验研究，综述仅用于查找文献。
2. 将新增论文和补充材料按 DOI 分文件夹保存；正文、补充材料和原始数据放在同一 DOI 文件夹。
3. 每批确认 10–20 篇论文的筛选清单。
4. 审核程序标记的冲突、高风险记录和随机抽查记录。
5. 查看每批质量报告；只有通过质量门槛的数据才进入机器学习表。

## 文件整理要求

建议结构：

```text
article/
  DOI文件夹/
    main.pdf
    supplementary.pdf
    raw_data.xlsx
    publisher.html
```

不要修改论文原始文件。修正内容填写在审核表中，保证之后能够复现数据来源和修改过程。

CSV 在部分 Windows Excel 环境中可能把 `Å`、`ø`、`±` 误显示成汉字。审核时使用 `.xlsx` 文件；不要再用 Excel 打开并另存原 CSV。
